{% extends 'intranet/base.html.twig' %}

{% block content %}

<section>
  <div class="container-fluid">
    <header> 
      <h1 class="display">Calendari</h1>
    </header>
    <div class="form-group row">
      <div class="col-lg-12">
        <a href="/intranet/calendar/create" class="btn btn-primary"><i class="fa fa-plus"></i> Nou acte</a>
      </div>
    </div>
    <div class="line"></div>
    <div class="form-group row">
      <div class="col-lg-12">
        <div class="card">
          <div class="card-header">
            <h4>Actes programats</h4>
          </div>
          <div class="card-body">
            
            <div id="intranet-calendar"></div>
                  
                {# <tbody>
                  {% for event in events %}
                    <tr>
                      <td>{{event.ts |date('d/m/Y') }}</td>
                      <td>{{event.title}}</td>
                      <td>{{event.ts |date('H:i') }}</td>
                      <td>{{event.address}}</td>
                      <td>{{event.city}}</td>
                      <td>{{event.description}}</td>
                      <td>
                        <button type="button" class="btn btn-info"><i class="fa fa-pencil"></i></button>
                        <button type="button" class="btn btn-danger"><i class="fa fa-trash"></i></button>
                      </td>
                    </tr>
                  {% endfor %}
                </tbody> #}
            </div>
          </div>
        </div>
      <div>
    </div>
  </div>
</section>
{% endblock %}

{% block scripts %}
  {{ encore_entry_script_tags('intranet/calendar/calendar') }}
{% endblock %}
